@model ChangeRoleViewModel
@{
    ViewData["Title"] = "Promote";
}

<h1>Promote</h1>

@Model.currentRole
@{Layout = "~/Views/Shared/_Layout.cshtml"; }
@*<div class="form-group w-50 mx-auto">
    <select class="custom-select" asp-for="allRoles">
        <option selected disabled>-- Choose An Option --</option>
        @foreach (var item in Model.allRoles)
        {
            <option value="@item">@item</option>
        }
    </select>
</div>*@

<div class="row text-white">
    <div class="col-md-4">
        <form method="post" asp-area="Magician" asp-controller="User" asp-action="Promote">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="newRole" class="control-label">asdasdasdasd</label>
                <select asp-for="newRole" asp-items="@Model.allRoles" class="form-control">
                    <option disabled selected value="">Choose an option</option>
                </select>
                <span asp-validation-for="allRoles" class="text-danger"></span>
            </div>
            <div hidden="hidden">
                <input value="@Model.currentRole" asp-for="currentRole"/>
            </div>
            <div class="form-group">
                <input type="submit" value="Change Role" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>